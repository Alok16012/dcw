import './globals.css';
export const metadata={title:'DCW — Your next move, made visible',description:'Compare verified courses, colleges and jobs with clear guidance from DCW.'};
export default function Layout({children}){return <html lang="en"><body>{children}</body></html>}
